import { Hind, Playfair } from 'next/font/google'

export const hind = Hind({
  weight: ['400', '500'],
  subsets: ['latin'],
})

export const playfair = Playfair({
  weight: '400',
  subsets: ['latin'],
})
